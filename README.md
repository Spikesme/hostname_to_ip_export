dieses Skript gibt die IP Adressen von Computerobjekten aus dem AD zurück. 
Die Objekte können durch ein Komma getrennt aufgelistet werden. 
Export als csv möglich


this script returns the IP addresses of computer objects from the AD. The objects can be listed separated by a comma. Export as csv possible
